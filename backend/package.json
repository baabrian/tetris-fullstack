{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend server for tetris",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:build": "tsc -w",
    "start:run": "nodemon ./build/index.js",
    "start": "concurrently \"npm run start:build\" \"npm run start:run\""
  },
  "author": "brian",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9"
  }
}
